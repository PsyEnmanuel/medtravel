{
  "name": "cm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start:admin": "npm run start --prefix ./admin",
    "start:server": "npm run start --prefix ./server",
    "build": "npm run build --prefix ./admin && npm run build --prefix ./server && git add . && git commit -m deploy && git push origin master",
    "deploy": "git add . && git commit -m deploy && git push origin master",
    "production": "git add . && git commit -m deploy && git push production master",
    "build:admin": "npm run build --prefix ./admin",
    "build:server": "npm run build --prefix ./server"
  },
  "author": "",
  "license": "ISC",
  "volta": {
    "node": "18.19.0"
  },
  "dependencies": {
    "vue-cal": "^4.8.1"
  }
}
